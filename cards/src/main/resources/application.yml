server:
 port: 9000
spring:
 application:
  name: cards
 profiles:
  active: "prod"
 datasource:
  url: jdbc:mysql://192.168.186.128:5306/cardsdb
  username: cards
  password: password
 jpa:
  show-sql: true
 sql:
  init:
   mode: always
 config:
  import: "optional:configserver:http://localhost:8071/"

management:
 endpoints:
  web:
   exposure:
    include: "*"